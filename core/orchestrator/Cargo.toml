# SPDX-FileCopyrightText: 2026 JustPav
# SPDX-FileCopyrightText: 2026 SmoothTeam
#
# SPDX-License-Identifier: LGPL-3.0-or-later WITH LGPL-3.0-linking-exception

[package]
name = "upac-lib-orchestrator"
description = "Stage pipeline engine shared by upac-lib and upac-lib-setup: context, stages, rollback, process lock"
version.workspace = true

edition.workspace = true
rust-version.workspace = true

license.workspace = true

readme.workspace = true
homepage.workspace = true
repository.workspace = true

keywords.workspace = true
categories.workspace = true

include = ["/src", "lib.toml", "build.rs"]

[lints]
workspace = true

[lib]
name = "upac_orchestrator"

[dependencies]
upac-lib-types = { workspace = true }
upac-lib-abi   = { workspace = true }

nix      = { workspace = true }
tempfile = { workspace = true }

tokio = { version = "1", features = ["rt"] }

[build-dependencies]
toml = { workspace = true }
