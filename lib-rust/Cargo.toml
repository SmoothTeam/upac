[package]
name = "upac-lib"
version = "0.1.0"
edition = "2024"

crate-type = ["cdylib", "rlib"]

[dependencies]
upac-derive = { path = "../derive" }

serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v4"] }
libc = "0.2.186"
libmdbx = "0.6"
rmp-serde = "1"
